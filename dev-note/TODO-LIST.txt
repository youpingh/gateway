
Kids Learn Chinese (klc-angular - an Angular app)

KLC App
- Add a favicon to the site. ✅
- Usage button doesn’t work on iPad, the popup window doesn’t stay.  ✅
- Add an organization name to GitHub learn-from.  ✅
- Change the homepage of the blogger to redirect to the new address.  ✅
- 图片问题：舌，鼻，屁，奶，今，昨，明，（不是正方形，奶：缺图） ✅
- Speech to text error message : too low, move it up to share the column with the 你说的是 message.  ✅
- Change picture 眉 ✅
- Fix spelling error cyan from cyan  ✅
- Practice search ✅
- Practice mobile ✅
- Random greeting picture for multiple choice ✅
- Try OpenAI speech to text on KLC (proxy: STT, to simplified Chinese, get pinyin). OpenAI STT is not better than Google's. It sometimes returns transitional Chinese and there is no control of this. So keep using Google's STT for now  ✅
- Convert KLC to Angular project. ✅
- printer.ts（another component maybe a better approach) ✅
- practice component  ✅
- Mobile support. ✅
- deploy to Google, my account. ✅
- dynamically generates some images (date time) not working. ✅
- 娃 image is missing  ✅
- recording error message  ✅
- Clean up index.html (scripts no longer needed) ✅
- check in the new project, retire the old one. (klc-angular https://kids-learn-chinese-1956.uc.r.appspot.com/)
- Footer format, max min width ✅
- Cleanup the speech area for a new character✅
- Port this app to school (project: gwca-character) ✅
- Usage page and video for the school version.✅
- Firebase based authentication to support users from Google. Family members only.✅
- Move the AllWords and GreetingImages objects to Firebase.✅
- Add a sentence game ✅
- Language levels ✅
- Use HSK vocabulary (level 1 - 7) ✅
- Add progress report with Firebase and Google user.
- Support the customization of language levels and individual user using individual words collection.
- Word collection customization by user
- Testing page that connects to the progress record 
- Add a story with a shadow reading component （generated video？）
- Add a utility of making phrase/sentence 组词、造句

Re-categorize the characters

- add an image child object to the Word {hasImage, path, prompt} (run region to change the object structure) ❌
- re-categorize all the characters semantically. ideally, each category/level has less than 20 characters.  ✅
- use the new character list to test the KLC app, the sidebar may need to change to two columns. ✅
- use the new character list to test the KLC-Admin app. ✅

Words Collection Editor (klc-admin - an Angular app)

- edit character, auto-pinyin, words/phrases selection, etc. ✅
- upload words, categories, users, greeting-message collections from JSON files. ✅

Admin Tools (admin-tools - a Node.js app)

Database Management

- Download database collections to JSON files (backup, batch editing, etc.)
- Set custom claims (configure user permission, role)

Image Generation

- Update the category field of json-prompts/*.json files to its English name. e.g. 动物 -> Animal (create a map) ✅
- Update the category field of json-prompts/*.json files to be consistent with collection/all-words.json ✅
- Create a new createPrompts.js to read json-prompts/*.json files to create a .jsonl for each category by level ✅
- Review all the prompt builders ✅
- Review all prompts (1, 2) ✅
- Batch generate images for levels 1 and 2 ✅

- Generate images of levels  (3, 4, 5, 6, 7).

Merge the generated images to KLC App

- Update klc-admin/src/assets/data/categories.json with new category list  gen-descriptions/category-list.txt (add Chinese name for each category) ✅
- Update klc-admin/src/assets/data/words.json with collection/all-words.json ✅
- Backup the generated images to Drive and resize all of them to 640x640. ✅
- Add the images/chars folder to the .gitignore file to avoid checking the images in. ✅
- Update the klc-angular/src/assets/images/chars with all the newly generated and resized images. (Use the existing Color images) Additionally, add the Functional image folder.✅
- Review the code to be sure the new images are referenced correctly (check Functional, word.grammar, comment category)✅
- Update the database collections: categories, words. (klc-admin -> upload)✅
- Test - issues: changing level doesn't update the category list (fixed), setDenoting level 2 images are missing ✅
- Changes - add code to hide the category/level list when click anywhere
- Deploy and check-in (all three projects: klc-angular, klc-admin, admin-tools).
